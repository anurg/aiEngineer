[project]
name = "aiengineer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=3.0.0",
    "pydantic[email]>=2.13.4",
    "python-dotenv>=1.2.2",
    "streamlit>=1.62.0",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
